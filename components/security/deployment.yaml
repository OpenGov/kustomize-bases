---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: security
spec:
  selector: {}
  template:
    metadata:
      # FG_R00495: Pod seccomp profile should be set [Medium]
      #           https://docs.fugue.co/FG_R00495.html
      annotations:
        seccomp.security.alpha.kubernetes.io/pod: "runtime/default"
